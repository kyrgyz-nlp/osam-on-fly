API_KEY=replace_this_with_your_local_key
# Optional: Set to 'development' to bypass API key check if API_KEY is not set.
# Defaults to 'production' behaviour (strict check) if unset.
# APP_ENV=development